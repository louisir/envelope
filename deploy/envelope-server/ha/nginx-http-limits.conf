# Loaded in the nginx http context; limits apply only where explicitly referenced.
limit_req_zone $binary_remote_addr zone=envelope_v2_rate:1m rate=30r/s;
limit_conn_zone $binary_remote_addr zone=envelope_v2_connections:1m;
