<Window x:Class="Envelope.Windows.Views.PasscodeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="410" SizeToContent="Height" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False" Background="{DynamicResource Brush.Window}" Foreground="{DynamicResource Brush.Text}"
        Icon="/Envelope.Windows;component/Resources/AppIcon.png">
    <Window.Resources>
        <Style x:Key="Passcode.Input" TargetType="PasswordBox" BasedOn="{StaticResource {x:Type PasswordBox}}">
            <Setter Property="Height" Value="44" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="CaretBrush" Value="{DynamicResource Brush.Accent}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="InputBorder" CornerRadius="8" BorderThickness="1"
                                BorderBrush="{DynamicResource Brush.Border}" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource Brush.Accent}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <StackPanel Margin="26">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
            <Image Source="/Envelope.Windows;component/Resources/AppIcon.png" Width="36" Height="36" />
            <TextBlock x:Name="Heading" Margin="12,0,0,0" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
        </StackPanel>
        <TextBlock x:Name="Description" TextWrapping="Wrap" Foreground="{DynamicResource Brush.TextMuted}" Margin="0,0,0,16" />
        <Border x:Name="InitialHint" Padding="12" CornerRadius="8" Background="{DynamicResource Brush.AccentSoft}" Margin="0,0,0,16">
            <TextBlock Text="{DynamicResource Passcode.InitialHint}" TextWrapping="Wrap" />
        </Border>
        <TextBlock x:Name="CurrentLabel" Margin="0,0,0,6" />
        <PasswordBox x:Name="CurrentCode" MaxLength="6" FontSize="20" Style="{StaticResource Passcode.Input}" />
        <StackPanel x:Name="NewCodePanel" Visibility="Collapsed" Margin="0,14,0,0">
            <TextBlock Text="{DynamicResource Passcode.New}" Margin="0,0,0,6" />
            <PasswordBox x:Name="NewCode" MaxLength="6" FontSize="20" Style="{StaticResource Passcode.Input}" />
            <TextBlock Text="{DynamicResource Passcode.Confirm}" Margin="0,14,0,6" />
            <PasswordBox x:Name="ConfirmCode" MaxLength="6" FontSize="20" Style="{StaticResource Passcode.Input}" />
        </StackPanel>
        <TextBlock x:Name="ErrorText" Foreground="{DynamicResource Brush.Danger}" TextWrapping="Wrap" Margin="0,12,0,0" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="{DynamicResource Common.Cancel}" IsCancel="True" MinWidth="80" Margin="0,0,10,0" />
            <Button x:Name="Submit" IsDefault="True" MinWidth="90" Click="Submit_Click" Style="{StaticResource Button.Primary}" />
        </StackPanel>
    </StackPanel>
</Window>
